## Agent skills

### Issue tracker

Các issue được lưu trữ dưới dạng file markdown nội bộ trong thư mục `.scratch/`. Xem `docs/agents/issue-tracker.md`.

### Triage labels

Sử dụng bộ nhãn tiếng Việt (`cần-đánh-giá`, `cần-thêm-thông-tin`, `sẵn-sàng-cho-ai`, `sẵn-sàng-cho-người`, `không-sửa`). Xem `docs/agents/triage-labels.md`.

### Domain docs

Dự án sử dụng cấu trúc một ngữ cảnh (Single-context) với file `CONTEXT.md` ở gốc. Xem `docs/agents/domain.md`.
